# Poll log

One row per poll, appended when the poll is approved. Counts stay blank until
the poll has run; then write them in as `Option 4 · Option 11 · …` and add
the takeaway. A research row with no counts is unfinished.

| Date | Kind | Lang | Question | Options and counts | Takeaway or decision |
|---|---|---|---|---|---|
